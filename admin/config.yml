backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: content/media
public_folder: content/media
collections:
  - name: "database"
    label: "Database"
    default: "/content/data.ndtl"
